services:
  ollama:
    image: ollama/ollama:latest
    container_name: secondbrain-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - secondbrain-network

volumes:
  ollama-data:

networks:
  secondbrain-network:
    driver: bridge
